/**
 * 全局配置
 * 部署前请修改为你自己的仓库信息
 */
window.SITE_CONFIG = {
  owner: 'MatildaHan',           // GitHub 用户名
  repo: 'MatildaHan.github.io',  // 仓库名
  branch: 'main',                // 分支名（新仓库通常是 main，旧仓库可能是 master）
  apiBase: 'https://api.github.com',
  rawBase: 'https://raw.githubusercontent.com'
};
